<%= form_for(@content_form) do |f| %>
  <% if @content_form.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@content_form.errors.count, "error") %> prohibited this content_form from being saved:</h2>

      <ul>
      <% @content_form.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :title %><br />
    <%= f.text_field :title %>
  </div>
  <div class="field">
    <%= f.label :content %><br />
    <%= f.text_area :content %>
  </div>
  <div class="field">
    <%= f.label :date %><br />
    <%= f.datetime_select :date %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
